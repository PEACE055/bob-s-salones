# Database
spring.datasource.url=jdbc:mysql://localhost:3306/salon_db?useSSL=false
spring.datasource.username=root
spring.datasource.password=Sujal@123

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable initialization if using programmatic approach
spring.sql.init.mode=never

# Optional: Set port
server.port=8080



spring.application.name=salonappointment

# Initialize the database with sample data

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Disable SQL initialization (we'll do it programmatically)

# Server Port


# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000
